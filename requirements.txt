# Runtime requirements for the Materials Science MCP server.
# This project is managed with uv. Prefer `uv sync` to install from
# pyproject.toml and uv.lock; this file is kept for tools that expect
# a requirements.txt document.

fastmcp>=0.2.0
playwright>=1.40.0
litellm>=1.50.0
openai>=1.0.0
python-dotenv>=1.0.0
aiofiles>=0.8.0
httpx>=0.25.0
structlog>=23.0.0
